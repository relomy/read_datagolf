# Shared state directory (contains contests.db)
DFS_STATE_DIR=/path/to/dfs-state

# Optional: override the Google Sheet target
SPREADSHEET_ID=1abcdef...

# Toggle reading the live contest via shared state
DG_USE_CONTEST_STATE=1

# Optional: store raw API responses for debugging
DG_SAVE_API=0
